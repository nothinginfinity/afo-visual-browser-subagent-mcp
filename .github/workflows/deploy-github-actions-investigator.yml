name: GitHub Actions Investigator

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - .github/workflows/deploy-github-actions-investigator.yml
      - subagents/github-actions-investigator/**

permissions:
  contents: read

concurrency:
  group: deploy-github-actions-investigator
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy afo-github-actions-investigator-mcp
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Deploy Worker
        shell: bash
        run: npx wrangler@latest deploy --config subagents/github-actions-investigator/wrangler.jsonc
