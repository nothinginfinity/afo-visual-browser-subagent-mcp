name: Deploy Receipt Investigator

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - .github/workflows/deploy-receipt-investigator.yml
      - subagents/deploy-receipt-investigator/**

permissions:
  contents: read

concurrency:
  group: deploy-receipt-investigator
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy afo-deploy-receipt-investigator-mcp
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Deploy Worker
        shell: bash
        run: npx wrangler@latest deploy --config subagents/deploy-receipt-investigator/wrangler.jsonc
