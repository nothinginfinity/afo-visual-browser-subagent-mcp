{
  "name": "afo-visual-browser-subagent-mcp",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/worker.js && node --check src/artifacts.js && node --check src/capture.js",
    "test": "node --test test/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.0.2"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
