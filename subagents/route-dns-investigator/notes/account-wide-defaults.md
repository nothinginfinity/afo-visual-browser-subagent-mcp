# Account-wide defaults

Runtime values to keep on the Route DNS Investigator Worker:

ACCOUNT_SUBDOMAIN should be jaredtechfit.
DEFAULT_ZONE_NAME should be agentfeedoptimization.com.
DEFAULT_SCAN_ALL_ZONES should be true.

The Worker source defaults account-wide scanning to true when the last runtime value is absent.
